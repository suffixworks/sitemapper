// Guest read-only view + comments, mobile-first. Data via /api/share/[token]. TODO(Phase 4/5).
export default function GuestSharePage() {
  return (
    <div className="p-8 text-sm text-muted-foreground">
      Shared sitemap — coming in Phase 4.
    </div>
  );
}
