// Staff editor: SitemapEditor + CommentsPanel for one sitemap. TODO(Phase 1/3/5).
export default function EditorPage() {
  return (
    <div className="p-8 text-sm text-muted-foreground">Editor — coming in Phase 1.</div>
  );
}
