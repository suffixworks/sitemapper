// Zustand store (+ zundo temporal middleware for undo/redo).
// Holds the tree (single source of truth); RF nodes/edges are projected each render.
// TODO(Phase 1): create the store; TODO(Phase 2): wrap with zundo.
export {};
