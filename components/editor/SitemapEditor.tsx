"use client";

// React Flow canvas (client-only). Renders nodes/edges projected from the tree.
// TODO(Phase 1): <ReactFlow> with non-draggable nodes + smoothstep edges.
export function SitemapEditor() {
  return null;
}
