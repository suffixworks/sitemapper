// Server-only: validate a share token (exists / not revoked / not expired) and
// resolve it to the share row + sitemap using the service-role client.
// TODO(Phase 4): implement validateShareToken().
export {};
