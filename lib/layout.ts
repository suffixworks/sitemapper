// Pure projection: SitemapDoc -> React Flow nodes/edges via d3-hierarchy.
// Node positions are DERIVED here on every structural change; never user-set.
// TODO(Phase 1): implement layout().
export {};
