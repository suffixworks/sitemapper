// SERVER-ONLY service-role client for guest routes (share-token flow).
// Uses SUPABASE_SERVICE_ROLE_KEY. NEVER import this from client code.
// TODO(Phase 4): createClient with the service-role key.
export {};
